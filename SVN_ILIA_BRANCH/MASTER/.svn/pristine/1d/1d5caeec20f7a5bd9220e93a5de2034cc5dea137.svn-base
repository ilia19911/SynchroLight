#define MAIN


#include "include.h"


timeout_t button_tim;

int main (void)
{
	initLowLevel ();     
	randomizer_init ();	
	systick_timer_init ();
	fileStorageInit ();
	rf_init ();
	display_set_string("0000");
	//player_play_mem(0, 0, 0);  /// TEST///////////////////////////////

/* ============== INSERT 1 ============== */	
	software_timer_start (&button_tim, 100);
/* ============== INSERT 1 ============== */	
	
		RCC_ClocksTypeDef sCLK;

		RCC_GetClocksFreq (&sCLK);
		
	while (1)
	{
		dislay_Handler();
		payer_handler();
		rf_task ();
		buttonTask();
		
		


/* ============== INSERT 2 ============== */
		
/* ============== INSERT 2 ============== */
	}
}
