#define MAIN

#include "include.h"

int main (void)
{
	initLowLevel ();     
	randomizer_init ();	
	systick_timer_init ();
	fileStorageInit ();

	
	led_maping_init_leds(static_led);// РЅР°СЃР·РЅР°С‡РµРЅРёРµ РјР°СЃСЃРёРІР° СЃРІРµС‚РѕРґРёРѕРґРѕРІ РґР»СЏ РјР°С‚СЂРёС†С‹
	led_maping_set_map();			 // РЅР°Р·РЅР°С‡РµРЅРёРµ РјР°С‚СЂС†С‹ СЃРІРµС‚РѕРґРёРѕРґРѕРІ. РџРѕРєР° С‚РµСЃС‚РѕРІРѕ РІС‹С‡РёС‚С‹РІР°РµРј РёР· СЃС‚Р°С‚РёС‡РµСЃРєРѕРіРѕ РјР°СЃСЃРёРІР°
	
	ws2812b_Init(&my_ws2812_3,70,105); // для устранения следствий бага. третий поток ленты не инициализируется, если не запустить функцию инициализации второй раз


	player_play_mem(0, 0, 0); 

/* ============== INSERT 1 ============== */	

/* ============== INSERT 1 ============== */	
		
	while (1)
	{
		//transceiverTask ();
		rf_task();
		payer_handler();

/* ============== INSERT 2 ============== */

		
/* ============== INSERT 2 ============== */
	}
}


